# ── AI Meeting Assistant ── requirements.txt ──────────────────────────────────
# Python >= 3.10 recommended

# ── Audio / Video acquisition ─────────────────────────────────────────────────
yt-dlp>=2024.4.9                  # Download audio from YouTube URLs
# All audio conversion/chunking uses the ffmpeg binary directly (streaming,
# zero-RAM); no Python audio library needed.

# ── LLM Orchestration — LangChain LCEL + Mistral ──────────────────────────────
langchain-core>=0.2.0
langchain-mistralai>=0.1.0        # LangChain wrapper for Mistral API
langchain-text-splitters>=0.2.0   # Directly imported (summarize.py, vector_store.py)
mistralai>=1.0.0                  # Official Mistral Python client (1.x required by langchain-mistralai)

# ── RAG Pipeline & Embeddings ─────────────────────────────────────────────────
sentence-transformers>=3.0.0      # HuggingFace embedding models (pulls torch, huggingface-hub, transformers)
langchain-huggingface>=0.0.3      # LangChain ↔ HuggingFace embeddings bridge
langchain-qdrant>=0.1.0           # Qdrant in-memory vector store

# ── Cloud APIs & UI ───────────────────────────────────────────────────────────
streamlit>=1.41.0                 # Web app framework (st.pills requires 1.41+)

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0              # Load API keys from .env
requests>=2.32.0                  # HTTP client for Groq/Sarvam STT APIs
tenacity>=8.2.0                   # Directly imported in core/transcriber.py (retry logic)
